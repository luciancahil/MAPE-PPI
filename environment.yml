name: MAPE-PPI

channels:
  - pytorch
  - conda-forge
  - defaults

dependencies:
  - python=3.8
  - pytorch=2.0.0
  - cudatoolkit=11.7
  - numpy=1.20.1
  - pip
  - pip:
      - nni==2.10
      - dgl==0.9.1